# 작업 진행 상황 (완료)

**시작일**: 2026-04-26
**완료일**: 2026-04-26
**대상 파일**: index.html (3527줄 → 4188줄)
**백업**: index.html.backup_20260426_221631
**중간 백업**: index.html.checkpoint_5done

## 완료된 작업

| # | 작업 | 상태 | 핵심 변경 |
|---|---|---|---|
| 1 | 디자인 B 배지 (시간 강조 + en-dash) | ✅ | `.badge-time` 12.5px/700/opacity 1.0, `~` → `–` |
| 2 | 이미지 저장 자동 전처리 (방안 A) | ✅ | `.exporting` 클래스 토글, fonts.ready, reflow 보장 |
| 3 | 이전 달 단가 자동 승계 | ✅ | 기존 로직 + 안내 배너 (`#prev-rate-notice`) |
| 4 | 이전 달 청구서 조회 (팝오버) | ✅ | `togglePrevBillPopover()` + 차이 비교 표시 |
| 5 | 발송 상태 + 결제선생 입력 컬럼 | ✅ | `is_sent_kakao`, `is_sent_payapp` 필드, 필터 4종 + 카운터 |
| 6 | 형제 묶음 (진입점, 토글, 통합 청구서) | ✅ | `familyId` 필드, 학생 카드 🔗 버튼, 가족 그룹 합계 행 |

## 새로 추가된 데이터 필드

### 학생(student)
- `familyId`: 형제 그룹 ID (없으면 외동)

### 수업료(tuitionData)
- `is_sent_kakao`: 카카오톡으로 청구서 이미지 발송 완료 여부
- `is_sent_payapp`: 결제선생에 단가 입력 완료 여부

## 사용법

### 형제 묶기
1. 시간표 화면에서 학생 카드의 🔗(링크) 아이콘 클릭
2. 같이 묶을 학생 선택 → "묶기"

### 형제 묶음으로 청구서 보기
1. 수업료 탭 → 요약 보기
2. 우측 상단 "👨‍👩‍👧 형제 묶음 보기" 토글 ON
3. 가족 합계 행에서 "통합 청구서 이미지" 버튼 클릭

### 상태 추적
- 수업료 목록 헤더에서 "📤 카톡 미발송", "💳 결제선생 미입력", "💰 미납" 필터로 빠르게 확인
- 각 행의 ✓/✕ pill 클릭으로 토글

### 이전 달 청구서 보기
- 학생의 청구서 모달 열면 우측 상단에 "이전 달" 버튼
- 클릭 → 팝오버로 이전 달 단가/청구액 비교

## 백업 파일 정리 (선택)
모든 작업이 잘 동작하는 것을 확인하면 백업 파일을 삭제해도 됩니다:
```
rm index.html.backup_20260426_221631
rm index.html.checkpoint_5done
```
